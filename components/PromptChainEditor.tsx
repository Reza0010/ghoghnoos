import React from 'react';

// This component is no longer in use. The functionality has been moved to PromptChainStudio.tsx
// as a specific, guided workflow instead of a generic chain editor.
const PromptChainEditor: React.FC = () => {
  return null;
};

export default PromptChainEditor;
