# 🦅 سیستم فروشگاهی هوشمند ققنوس (نسخه ۱۱.۰)
### پنل مدیریت حرفه‌ای + ربات تلگرام و روبیکا

این پروژه یک راهکار کامل برای فروش محصولات دیجیتال و فیزیکی در پلتفرم‌های تلگرام و روبیکا است که با یک پنل مدیریت دسکتاپ (PyQt6) کنترل می‌شود.

---

## 🚀 ویژگی‌های کلیدی
- **داشبورد مانیتورینگ**: نمایش لحظه‌ای آمار فروش، درآمد، سفارشات و نمودارهای رشد.
- **ربات دوقلو**: مدیریت همزمان ربات تلگرام و روبیکا با یک دیتابیس واحد.
- **سیستم تیکتینگ**: پشتیبانی مستقیم مشتریان از داخل پنل مدیریت.
- **درگاه پرداخت**: اتصال به زرین‌پال و مدیریت کارت‌های بانکی برای پرداخت کارت به کارت.
- **انبارداری هوشمند**: دسته‌بندی محصولات، مدیریت موجودی و هشدار اتمام کالا.
- **CRM پیشرفته**: مدیریت مشتریان، تعیین سطح (VIP/Active)، یادداشت‌گذاری و خروجی اکسل.
- **امنیت بالا**: ورود دومرحله‌ای به پنل، رمزنگاری تنظیمات و سیستم بک‌آپ‌گیری خودکار.
- **شخصی‌سازی کامل**: تغییر توکن‌ها، پروکسی، متن‌های خوش‌آمدگویی و بنرها از داخل پنل.

---

## 🛠 پیش‌نیازها
- **Python 3.10+** (تست شده روی 3.13)
- کتابخانه‌های مورد نیاز:
  ```bash
  pip install pyqt6 qasync qtawesome matplotlib htmldoc asyncio python-telegram-bot aiohttp sqlalchemy arabic-reshaper python-bidi
  ```

---

## 💻 نحوه اجرا
۱. ابتدا فایل `config.py` یا تنظیمات دیتابیس را بررسی کنید.
۲. برای اجرای همزمان پنل و ربات‌ها، فایل اصلی را اجرا کنید:
   ```bash
   python run_panel.py
   ```
۳. رمز عبور پیش‌فرض پنل: `admin`

---

## ⚙️ تنظیمات اولیه
پس از ورود به پنل، به بخش **تنظیمات > تنظیمات پایه** بروید و موارد زیر را وارد کنید:
- **Telegram Bot Token**: توکن دریافتی از BotFather
- **Rubika Bot Token**: توکن دریافتی از بخش توسعه‌دهندگان روبیکا
- **Proxy**: در صورت نیاز به پروکسی (برای ایران)، آدرس را وارد و فعال کنید.
- **Admin IDs**: آیدی عددی تلگرام خود را وارد کنید تا به منوی مدیریت در ربات دسترسی داشته باشید.

---

## 📄 گزارشات
شما می‌توانید در بخش داشبورد، با کلیک روی **گزارش پیشرفته**، فایل PDF عملکرد مالی فروشگاه را دریافت کنید.

---
**توسعه داده شده برای ارائه تجاری و حرفه‌ای.**
